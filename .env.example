# Environment variables for Solana Agent Swarm

# Solana Configuration
RPC_URL=https://api.devnet.solana.com
PROGRAM_ID=Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS
IDL_PATH=./target/idl/agent_swarm.json

# Agent Keypair Paths (generate these with solana-keygen)
CONSENSUS_AGENT_KEYPAIR=./keys/consensus-keypair.json
ANALYTICS_AGENT_KEYPAIR=./keys/analytics-keypair.json
EXECUTION_AGENT_KEYPAIR=./keys/execution-keypair.json
RISK_AGENT_KEYPAIR=./keys/risk-keypair.json
LEARNING_AGENT_KEYPAIR=./keys/learning-keypair.json

# Authority (for initialization)
AUTHORITY_KEYPAIR=./keys/authority-keypair.json

# Swarm Parameters
MAX_AGENTS=5
MIN_VOTES_REQUIRED=3
PROPOSAL_TIMEOUT=3600

# Simulation Mode (set to true for testing without real funds)
SIMULATION_MODE=true

# Logging
LOG_LEVEL=INFO
